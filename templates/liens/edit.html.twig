{% extends 'base.html.twig' %}

{% block title %}Modifier le lien{% endblock %}

{% block body %}
<h1>Modifier le lien</h1>

<div class="edit-container">
    {{ include('liens/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="edit-buttons">
        <a href="{{ path('app_liens_index') }}" class="edit-btn">Retour à la liste</a>
        {{ include('liens/_delete_form.html.twig', { 'class': 'edit-delete-btn' }) }}
    </div>
</div>
{% endblock %}
